# Copy to the server ONLY (never commit the real file):
#   /etc/cloudflare/credentials.ini
#   /root/.secrets/cloudflare.ini   (optional mirror)
#
# chmod 600 on both. Token needs Zone:DNS:Edit for hexalyte.com.
dns_cloudflare_api_token = REPLACE_WITH_CLOUDFLARE_API_TOKEN
